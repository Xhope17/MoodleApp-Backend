# ===== CONFIGURACIÓN DEL SERVIDOR =====
# Puerto donde correrá el servidor backend (por defecto 3000)
PORT=3000

# URL base de tu instalación de Moodle
# Formato: http://TU_IP_O_DOMINIO/CARPETA_MOODLE
# Ejemplo: http://192.168.1.100/moodle o http://moodle.midominio.com
# Para obtener tu IP local en Windows: ipconfig (busca IPv4 Address)

MOODLE_BASE=http://TU_IP_AQUI/moodle

# Nombre del servicio web configurado en Moodle
# Para crearlo: Administración del sitio > Servidor > Servicios web > Servicios externos
# Debe tener habilitadas las funciones necesarias de la app
MOODLE_SERVICE=moodle_mobile_app

# --------------------------------- GOOGLE OAUTH (Opcional) ----------------------------------------------
# Solo necesario si usas autenticación con Google
# Obtener en: https://console.cloud.google.com/ > APIs & Services > Credentials
# 1. Crear un proyecto nuevo o seleccionar uno existente
# 2. Ir a "Credentials" > "Create Credentials" > "OAuth 2.0 Client ID"
# 3. Tipo: Web Application
# 4. Agregar URLs autorizadas de redirección:
#    - http://localhost:3000/auth/google/callback
#    - http://localhost:8081
# 5. Copiar el Client ID y Client Secret aquí
GOOGLE_CLIENT_ID= (TU_CLIENT_ID_AQUI.apps.googleusercontent.com)
GOOGLE_CLIENT_SECRET= (TU_CLIENT_SECRET_AQUI)



# Token de administrador de Moodle (opcional)
# Solo necesario si quieres crear usuarios automáticamente desde Google
# Para obtenerlo: Moodle > Administración del sitio > Seguridad > Llaves de seguridad
# Si no lo configuras, los usuarios deben existir en Moodle con el mismo email de Google
MOODLE_ADMIN_TOKEN= (TU_ADMIN_TOKEN_AQUI)

# Eso seria todo 